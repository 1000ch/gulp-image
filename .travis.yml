language: node_js
node_js:
  - '12'
  - '10'
  - '8'
notifications:
  email: false
branches:
  only:
    - master
addons:
  apt:
    packages: nasm
before_install: if [[ $TRAVIS_OS_NAME == osx ]]; then brew update && brew install nasm; fi;
